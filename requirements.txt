# Backend dependencies (AGENTS.md §6, §7).
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pyyaml>=6.0
networkx>=3.2
# Testing
pytest>=8.0
httpx>=0.27
# Optional / later-sprint dependencies (not required for Sprint 0):
# openai-compatible providers use the existing httpx dependency
# elevenlabs
# qdrant-client
# crawl4ai
# scrapling
# tavily-python
